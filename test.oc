Prayer: St. Porphyrios, please pray for us

/pray/ this is an example program which tests all of the functionalities of the language

invoke HolyTrinity() {

    chant("In the Name of the Father, Son, And The Holy Spirit.");

    incense variable = "Now And Forever and unto the ages of ages. Amen.";
    chant("%s", variable);

    chant("Result of equation: ");
    theosis(2 + 5 - 2 / 7 * 10);
    chant("Now an equation should be ran but with no output.");
    theosis(15 + 15); > null

    repent("Enter a random string: ", str);
    chant("You entered: %s", str);
}

/pray/ unceasingly.pray: HolyTrinity

call.upon HolyTrinity
